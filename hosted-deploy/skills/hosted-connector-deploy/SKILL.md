---
name: hosted-connector-deploy
description: "DEPRECATED. Use MintMCP's Admin MCP for everything related to deploying and managing hosted connectors on MintMCP."
---

# Deploy MCP Servers as Hosted Connectors (Deprecated)

> **This skill is deprecated.** Use **MintMCP's Admin MCP** for everything
> related to hosted connectors — Dockerfile generation, image build, deploy,
> updates, env var configuration, and OAuth setup.
